Игра "<%= @game.name %>"

<br />
<br />
<% if @game.draft? %>
  <div class="is-draft-message">Черновик</div>
<% end %>
<br />

Автор - <%= @game.author.nickname %><br /><br />

<% unless @game.starts_at.nil? %>
  Начало игры: <%= @game.starts_at.strftime("%Y-%m-%d %H:%M") %>
<% else %>
  Дата начала игры ещё не назначена
<% end %>

<br />
<br />
<%= @game.description %><br />
<br />
<% if logged_in? and @current_user.author_of?(@game) %>
  Задания:
  <br />
  <%= partial 'levels/list', :levels => @game.levels  %>
  <br />
<% end %>
<br />
<%= link_to "Редактировать эту игру", resource(@game, :edit) if logged_in? and @current_user.author_of?(@game) and ! @game.started? %> <br />
<%= link_to "Добавить в неё новое задание", resource(@game, :levels, :new) if logged_in? and @current_user.author_of?(@game) and ! @game.started? %>